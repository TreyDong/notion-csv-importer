services:
  notion-csv-importer:
    image: treydong/notion-csv-importer:latest
    ports:
      - "8002:8000"
    volumes:
      - ./.env:/app/.env:ro
    env_file:
      - ./.env
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped